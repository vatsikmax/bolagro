{
  "name": "bolagro",
  "private": true,
  "description": "bolagro e-commerce store",
  "workspaces": {
    "packages": [
      "frontend",
      "backend",
      "prom"
    ]
  },
  "scripts": {
    "lint": "yarn workspaces lint",
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vatsikmax/bolagro.git"
  },
  "packageManager": "yarn@4.6.0",
  "lint-staged": {
    "*.{ts}": [
      "prettier --write",
      "eslint --fix --cache"
    ]
  }
}
